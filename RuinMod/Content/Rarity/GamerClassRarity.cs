/*using Microsoft.Xna.Framework;
using Terraria.ModLoader;

namespace RuinMod.Content.Rarity
{
    internal class GamerClassRarity : ModRarity
    {
        public override Color RarityColor => new Color(200, 175, 39);

        public override int GetPrefixedRarity(int offset, float valueMult)
        {
            if (offset > 0)
            {
                return ModContent.RarityType<GamerClassHigherRarity>();
            }

            return Type;
        }
    }
}*/
