/*using Microsoft.Xna.Framework;
using Terraria;
using Terraria.ModLoader;

namespace RuinMod.Content.Rarity
{
    internal class GamerClassHigherRarity : ModRarity
    {
        public override Color RarityColor => new Color(247, 255, 169);

        public override int GetPrefixedRarity(int offset, float valueMult)
        {
            if (offset < 0)
            {
                return ModContent.RarityType<GamerClassRarity>();
            }

            return Type;
        }
    }
}*/
